<Window x:Class="HelloWPF.ExerciseTwoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWPF"
        mc:Ignorable="d"
        Title="ExerciseTwoWindow" Height="300" Width="300">
    <Grid>
        <Canvas>
            <Ellipse Panel.ZIndex="1" Fill="Aqua" Canvas.Left="50" Canvas.Top="50" Width="40" Height="80"/>
        </Canvas>

        <Button Click="Get_Area" Width="100" Height="20">
            Beräkna area
        </Button>
    </Grid>
</Window>
